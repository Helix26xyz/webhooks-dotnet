---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: defaultconnection-data
  labels:
    app: defaultconnection
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  # Optional: Specify storageClassName if you have a specific storage class
  # storageClassName: standard
